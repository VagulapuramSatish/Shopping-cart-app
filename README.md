E-Commerce Platform (Go + Gin & React + Vite)
A simple e-commerce platform built with Go (Gin framework) for the backend and React (Vite) for the frontend. The platform allows users to signup, login, create carts, add items, and place orders.
This project demonstrates JWT authentication, ORM usage with GORM, and a single-session login system.
